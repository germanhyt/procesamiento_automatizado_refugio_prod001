# Producción — alineado con config/.env.production (solo URLs públicas; sin secretos).
# API_URL allí es https://…/api; el cliente Expo usa la base sin /api (axios añade /api).

EXPO_PUBLIC_API_URL=https://api.datarefugio.gcbprojects.site
EXPO_PUBLIC_WS_URL=wss://api.datarefugio.gcbprojects.site
