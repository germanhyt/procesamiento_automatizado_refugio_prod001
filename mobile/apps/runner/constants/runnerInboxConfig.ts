/** Límites de la bandeja in-memory (no persistida). */
export const RUNNER_INBOX_MAX_ITEMS = 80;
/** Antiduplicado entre WS y push para el mismo pedido/driver (ms). */
export const RUNNER_INBOX_DEDUPE_MS = 45_000;
