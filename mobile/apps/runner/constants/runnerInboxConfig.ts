/** Límite de ítems en bandeja (solo memoria; la historia larga está en API). */
export const RUNNER_INBOX_MAX_ITEMS = 80;
/** Antiduplicado entre eventos WS para el mismo pedido/driver (ms). */
export const RUNNER_INBOX_DEDUPE_MS = 45_000;
